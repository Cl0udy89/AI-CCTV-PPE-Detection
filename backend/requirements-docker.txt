# Docker-specific requirements:
# - opencv-python-headless (no X11 dependencies)
# - torch/torchvision excluded here — installed separately in Dockerfile
#   to use the correct CPU or CUDA wheels and benefit from layer caching

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0
opencv-python-headless>=4.9
numpy>=1.26
ultralytics>=8.2.18
lapx>=0.5.2
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
reportlab>=4.1
openpyxl>=3.1
